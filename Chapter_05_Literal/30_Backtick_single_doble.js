let name = "Supriya";

let single = 'Hello, ' + name;   // Single quotes - needs + for variables
let double = "Hello, " + name;   // Double quotes - same as single
let backtick = `Hello, ${name}`; // Backtick - can embed ${} directly

console.log(single);
console.log(double);
console.log(backtick);
