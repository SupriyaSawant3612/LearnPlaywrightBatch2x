let retryCount = 0;
retryCount = retryCount + 1;
retryCount = retryCount + 1;
console.log("RetryCount:", retryCount); // Output: 1

let retryCount = 5; // This will throw an error because retryCount is already declared
